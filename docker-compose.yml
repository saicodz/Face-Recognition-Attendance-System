version: "3.9"

services:
  api:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./storage:/app/storage
    environment:
      - ENV=production
      - JWT_SECRET_KEY=${JWT_SECRET_KEY:-change-me}
    restart: unless-stopped
